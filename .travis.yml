dist: trusty
language: go
go:
- "1.10"

install:
- go get github.com/mitchellh/gox
- go get -t ./...

script:
- go install ./...
- gox -output="bin/{{.Dir}}_{{.OS}}_{{.Arch}}" ./...

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file_glob: true
  file: bin/**/*
  skip_cleanup: true
  on:
    tags: true
